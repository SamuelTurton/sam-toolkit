# sam-toolkit

Python-first toolkit for scripts, CLI utilities, and SQL helpers.

## Dev setup

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -U pip
pip install -e .
pytest
sam-toolkit --version

